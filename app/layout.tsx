import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "Tuthaco: Leading Exporter of Agricultural Products in Thanh Hóa, Vietnam",
  description: "Discover Tuthaco, a pioneering company in agricultural product processing and export in Thanh Hóa, Vietnam. Established in 2003, we boast 20 years of experience in crafting high-quality handicrafts and agricultural products that meet international standards, providing stable jobs and contributing to sustainable agricultural development in Vietnam."
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
